
spring:
  datasource:
    url: jdbc:postgresql://localhost:3307/userservice
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-plateform: org.hibernate.dialect.PostgreSQLDialect


    url: jdbc:postgresql://localhost:3307/notificationservice
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver

springdoc:
  swagger-ui:
    path: /swagger-ui.html

eureka:
  instance:
    prefer-ip-address: true

  client:
  fetch-registry: true
  registry-with-eureka: true
  service-url:
    defaultZone: http://localhost:9191/eureka
